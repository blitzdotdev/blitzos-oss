export const meta = {
  name: 'tape-design-research',
  description: 'Research the BlitzOS session-tape design as the data substrate for a fleet-scale, mutable-objective auto-research loop over the codebase',
  whenToUse: 'Deep research + design for the BlitzOS session tape, derived downstream of the fleet re-simulation / code-space-optimization infra',
  phases: [
    { title: 'Research', detail: '6 parallel strands: codebase seams, trace standards, replay/determinism, objective layer, code-space optimization, fleet privacy/egress' },
    { title: 'Requirements', detail: 'derive auto-research infra requirements, then the tape requirements downstream of them' },
    { title: 'Designs', detail: '3 candidate tape architectures from distinct priors' },
    { title: 'Adversarial', detail: 'judge + skeptic + completeness critic break the candidates' },
    { title: 'Synthesize', detail: 'final recommendation: capture set + mutable-objective layer + replay strategy + storage/egress + milestones' },
  ],
}

const CONTEXT = [
  'CONTEXT: BlitzOS + the research question.',
  '',
  'BlitzOS is an Electron "agent OS": a desktop where a human and an AI agent collaborate on live "surfaces" (browser windows, notes, agent-authored HTML panels). The agent is a VISIBLE terminal process (Claude Code via "claude --resume", bootstrapped by src/main/agent-runtime.mjs, supervised by terminal-manager.mjs), woken by content-agnostic "perception moments" delivered over an HTTP /events long-poll. It drives the OS through a shared tool registry (src/main/os-tools.mjs, makeOsTools(ops)) bound to THREE transports: the Electron agent-socket relay (agentSocket.ts), an Electron localhost control server (control-server.ts via electron-os-tools.ts), and a server backend (preview/backend.mjs). Perception is a shared kernel (src/main/perception-core.mjs / events.mjs): DOM sensors (INJECT) feed raw signals coalesced into moments {seq,ts,surfaceId,url,title,trigger,signals,user}; setMomentTap(fn) fires on every emitted moment. Repo: /Users/minjunes/superapp/teenybase/agent-os (CLAUDE.md + plans/ describe it).',
  '',
  'There is an existing draft "session tape" spec (spec 01): one append-only JSONL log at ~/Blitz/.blitzos/log/session-<date>.jsonl recording every agent tool call (a withRecording(tools, tape) wrapper at each transport) + every perception moment (setMomentTap) + OS events; downstream "views" (action-ledger, correction log, intent-state) are reads over it. It captures EFFECTS (tool calls) and ENVIRONMENT (moments), clips heavy payloads at write, and is framed local-only/gitignored.',
  '',
  'THE ACTUAL GOAL (this reframes everything): the tape\'s PRIMARY consumer is a FLEET-SCALE AUTO-RESEARCH LOOP, not dashboards. The org collects tapes from MANY users and runs a re-simulation loop that OPTIMIZES OVER THE CODE-SPACE OF THE BLITZOS CODEBASE (the least-constrained search space): it searches for a codebase variant that, when re-run on the users\' tapes, best satisfies constraints the org GUESSES the user cares about (e.g. "aligned autonomy"). Crucially: (1) model WEIGHTS are FIXED; only the codebase/harness/context around the model changes. (2) The objective/constraint set is NOT fixed or exhaustive; it WILL CHANGE. So the training infra must allow efficient ADAPTATION to changing objectives, and the TAPE/TAP design must be DESIGNED DOWNSTREAM of this infra\'s requirements. The moat is this fleet data + re-simulation flywheel.',
  '',
  'Honest tensions to engage, not dodge: faithful replay wants maximal raw capture; fleet aggregation + the product\'s trust/legibility moat want privacy/consent/minimization; LLM nondeterminism plus a model that updates under you limits literal replay; re-running a NEW codebase on an OLD tape diverges immediately (distribution shift), so human verdicts attached to the old trajectory may not transfer.',
  '',
  'Style for all outputs: plain, decisive, technical. NO em dashes (use periods, commas, parentheses). Ground claims; name what is unknown. Cite real systems/papers. If web search is unavailable, use your own current knowledge and say so.',
].join('\n')

phase('Research')
log('Fanning out 6 research strands (codebase seams + 5 external/design strands).')
const STRANDS = [
  { key: 'codebase-seams', prompt: CONTEXT + '\n\nSTRAND: BlitzOS codebase reality and capturable seams. Explore the repo at /Users/minjunes/superapp/teenybase/agent-os. Map EXACTLY where the agent\'s full I/O and the environment flow, and what can be captured to faithfully REPLAY or FORK a run. Cover: (a) os-tools.mjs registry + its 3 transport bindings (agentSocket.ts, control-server.ts/electron-os-tools.ts, preview/backend.mjs) and the existing withRecording tap point; (b) the perception kernel (perception-core.mjs, events.mjs): moments, setMomentTap, INJECT/DRAIN sensors, raw vs coalesced data; (c) the agent runtime (agent-runtime.mjs): how the model CONTEXT is assembled (bootstrap.txt + chat.md tail + the /events wake frame + tool results) and whether the RAW prompt sent to the model and the RAW completion returned are observable ANYWHERE (the likely gap: the tape records tool-call EFFECTS but not the model\'s own prompt/completion, because the model runs inside an opaque claude terminal). If the raw model I/O is NOT accessible through the terminal, say so and enumerate fallbacks (terminal transcript jsonl under .blitzos/terminals/, a CLAUDE log dir, an OTel/proxy shim, reconstructing context from the same inputs). (d) What state must be snapshotted to FORK a run from a checkpoint (workspace files, surface/store state, relay/session ids). Deliverable: a concrete map of capture points that EXIST, capture points MISSING for replay, and the exact seams where new capture would attach. Read the real files; cite file:line.' },
  { key: 'trace-standards', prompt: CONTEXT + '\n\nSTRAND: LLM-agent trace/observability standards (prior art). Survey the state of the art and extract the consensus record/span schema + lessons for a tape meant for REPLAY+EVAL (not dashboards). Cover OpenTelemetry GenAI semantic conventions, OpenInference (Arize Phoenix), LangSmith, Braintrust, W&B Weave, Langfuse, Helicone, AgentOps, MLflow tracing. What fields do they standardize (messages/inputs/outputs, tool calls, token/cost, latency, span trees, session/thread ids, metadata)? What do they get RIGHT and what is MISSING for re-simulation (they capture for human inspection, not re-execution)? Use web search for current specs; cite versions. Deliverable: the consensus schema elements to adopt + the gaps we must fill.' },
  { key: 'replay-determinism', prompt: CONTEXT + '\n\nSTRAND: replay and counterfactual simulation of agent runs. The org wants to "re-run the BlitzOS codebase on a user tape." Enumerate the FLAVORS of replay and what each demands of the tape: (1) deterministic re-execution (mock/cassette LLM responses, response caching, seedable sampling, VCR-style record-replay) and its HARD limit when the codebase/prompt changes (the model is asked something new, so cached responses no longer apply); (2) comparative eval on FIXED situations (re-feed recorded inputs, sample fresh completions, score); (3) forked counterfactual rollouts (snapshot+restore the environment, let a changed harness diverge) and how to make the ENVIRONMENT re-executable or mockable (replay recorded tool results vs re-execute live, and how each handles divergence). Survey: VCR/cassettes, LLM record-replay libraries, agent gyms / replayable web envs (WebArena, VisualWebArena, WorkArena, AndroidWorld), system record-replay (rr, Hermit, Antithesis deterministic sim), and off-policy / counterfactual policy evaluation from logged data (importance sampling, doubly-robust OPE). Be brutally honest about what is and is not feasible with a fixed-but-updatable model. Deliverable: a taxonomy {replay mode -> capture requirements, fidelity, limits} and a recommendation for which mode(s) the tape should target.' },
  { key: 'objective-layer', prompt: CONTEXT + '\n\nSTRAND: turning tapes into a MUTABLE objective/label layer. We cannot change weights; we score CODEBASE variants by how well their re-run trajectories satisfy org-guessed, CHANGING constraints. Survey how traces become improvement signal and design a SWAPPABLE objective layer that sits OVER the tape so objectives change WITHOUT re-capturing: human-verdict-as-label (accept/edit/veto/undo on agent output = highest-signal supervised feedback), preference pairs (RLHF/DPO/KTO), process reward / step-level labels, LLM-as-judge rubrics, eval datasets built from production traces (LangSmith/Braintrust eval sets), regression/eval-driven development. Key questions: what must the tape store so an objective DEFINED LATER can still be computed over OLD tapes (capture must be objective-agnostic and rich)? How do you represent objectives as DATA (rubrics, judge prompts, label functions) rather than baked-in capture? How do you durably link a human verdict to the EXACT situation+action that produced it? How do new objectives that need signal the old capture never recorded get handled (re-capture vs derive vs degrade)? Deliverable: the objective-layer design + the precise capture requirements it imposes on the tape.' },
  { key: 'code-space-optim', prompt: CONTEXT + '\n\nSTRAND: code-space optimization / auto-research loop architecture. The org "optimizes over the code-space of the BlitzOS codebase" against an evaluator built from user tapes. Survey the generator-evaluator and evolutionary-search literature and map it onto this concretely: DSPy + optimizers (MIPRO, GEPA, BootstrapFewShot), prompt optimization (APE, OPRO), AlphaEvolve / FunSearch / OpenEvolve (LLM-guided code/program search against an evaluator), the AI-Scientist and self-improving-agent loops, eval-driven development, automated software-engineering agents (SWE-bench-style). End-to-end: propose codebase variant -> re-run on tapes -> score against the (mutable) evaluator -> select -> iterate. What makes the loop EFFICIENT given the evaluator/re-sim is the bottleneck: response caching, incremental/partial re-eval, parallel candidate eval, cheap proxy metrics before full eval, dataset subsampling/curriculum, early stopping. How do MUTABLE/MULTI objectives change the loop (Pareto fronts, re-scoring old runs under new objectives without re-running)? What is the REALISTIC restricted search space (not "the whole codebase" naively: prompts, tool definitions, policy modules, the harness around a fixed model)? Deliverable: a reference architecture for the loop and the precise DEMANDS it places on the data substrate. This strand is the primary requirements source the tape design must be downstream of.' },
  { key: 'fleet-privacy-egress', prompt: CONTEXT + '\n\nSTRAND: fleet aggregation, privacy, egress, scale, storage. Capturing rich user data (screen/page content, keystrokes, logged-in surfaces, model I/O) from MANY users into a central corpus collides with the product trust/legibility moat. Design the egress/privacy/scale posture: consent model + per-record egress flag (the spec\'s "shared"), redaction/PII scrubbing (at-write vs at-egress), the data-minimization vs max-capture-for-replay tension and how to reconcile (raw on-device, redacted/consented subset egressed; or capture-everything-encrypted; or federated re-sim where data never leaves), federated learning/analytics, differential privacy, secure enclaves / clean rooms, compliance (GDPR/CCPA, right-to-delete propagating into a training/eval corpus and any trained artifacts). Plus the storage substrate at fleet scale: append-only JSONL vs columnar (Parquet/Arrow) vs content-addressed blob store for large payloads (screenshots, page DOM), schema evolution over a long-lived multi-user corpus, dedup, indexing for the eval read-path, cost/volume estimates. Deliverable: the egress/privacy/scale/storage constraints + a recommended posture that preserves BOTH faithful replay value AND the trust moat.' },
]
const research = (await parallel(STRANDS.map((s) => () => agent(s.prompt, { label: 'research:' + s.key, phase: 'Research' }))))
const researchText = STRANDS.map((s, i) => '## STRAND: ' + s.key + '\n\n' + (research[i] || '(no result)')).join('\n\n---\n\n')

phase('Requirements')
log('Deriving auto-research infra requirements, then the tape requirements downstream of them.')
const requirements = await agent(CONTEXT + '\n\nYou are the requirements synthesizer. Below are 6 research strands. Produce TWO sections, in order:\n\n1) AUTO-RESEARCH INFRA REQUIREMENTS: a crisp, prioritized list of what the fleet re-simulation / code-space-optimization loop NEEDS to function and to adapt to CHANGING objectives (the loop, the evaluator, the data read-path, the mutable-objective layer, the re-sim execution model, scale/efficiency).\n\n2) TAPE/TAP REQUIREMENTS, DERIVED DOWNSTREAM of (1): exactly what the capture substrate must record, at what granularity, in what shape, stored how, with what egress/consent properties, so (1) is efficient AND objective-agnostic. Make the downstream derivation EXPLICIT: for each tape requirement, name which infra requirement forces it. Call out the hard tensions (replay fidelity vs privacy; max-capture vs honeypot; determinism vs a fixed-but-updatable model; one store vs many; distribution shift breaking verdict-transfer) and take a position on each.\n\nResearch strands:\n\n' + researchText, { label: 'requirements', phase: 'Requirements' })

phase('Designs')
log('Generating 3 competing tape architectures from distinct priors.')
const PRIORS = [
  { key: 'replay-first', prompt: 'PRIOR: maximal-fidelity REPLAY-FIRST. Optimize for the ability to re-execute/fork runs at the highest fidelity: capture full per-turn model I/O (assembled context in + raw completion out), tool args+results, perception moments, and periodic environment snapshots (workspace/surface/session state) to fork from checkpoints. Lean toward capturing everything raw and solving privacy at egress.' },
  { key: 'eval-corpus-first', prompt: 'PRIOR: EVAL-CORPUS-FIRST. Optimize for a clean, objective-agnostic corpus of (situation, agent-action, outcome, human-verdict) records that powers comparative eval and a swappable objective/judge layer, accepting weaker literal replay. Treat the corpus + judges as the product; "replay" means re-feed fixed situations and score fresh completions. Designed so a NEW objective re-scores OLD tapes with no re-capture.' },
  { key: 'federated-evolvable', prompt: 'PRIOR: PRIVACY/FEDERATED + SCHEMA-EVOLVABLE FIRST. Optimize for fleet scale under the trust moat: raw on-device, content-addressed big payloads, consent-gated redacted egress, a schema that evolves over a long-lived multi-user corpus, objectives re-computable over old data, and re-sim that can run on-device or in a clean room so the central honeypot is minimized.' },
]
const designs = (await parallel(PRIORS.map((p) => () => agent(CONTEXT + '\n\nYou are a tape architect. Using the REQUIREMENTS below, propose a CONCRETE BlitzOS session-tape design from this prior:\n\n' + p.prompt + '\n\nDeliver: (a) what is captured + exact record shape(s) with field names; (b) the tap points in BlitzOS (specific to the real seams: the three os-tools transports, setMomentTap, the agent terminal); (c) storage layout + egress/consent design; (d) which replay mode(s) it supports and how the auto-research loop consumes it; (e) how objectives stay MUTABLE over OLD tapes; (f) milestones (shippable slices, smallest de-risking first); (g) the top 3 tradeoffs/risks of THIS prior, stated honestly. Concrete enough to build from.\n\nREQUIREMENTS:\n\n' + requirements, { label: 'design:' + p.key, phase: 'Designs' }))))
const designsText = PRIORS.map((p, i) => '## DESIGN: ' + p.key + '\n\n' + (designs[i] || '(no result)')).join('\n\n---\n\n')

phase('Adversarial')
log('Judging + adversarially stress-testing the candidates.')
const critiques = await parallel([
  () => agent(CONTEXT + '\n\nYou are an impartial JUDGE. Score the 3 candidate tape designs below against: replay/re-sim fidelity; objective-mutability (can a NEW objective be computed over OLD tapes); eval/loop efficiency at fleet scale; privacy/egress/trust; fit to the REAL BlitzOS seams; implementation cost. Give each design a per-criterion score (1-5) with a one-line justification, name the single best element of EACH, and recommend a concrete SYNTHESIS (which elements to combine). Be specific.\n\nREQUIREMENTS:\n' + requirements + '\n\nDESIGNS:\n' + designsText, { label: 'judge', phase: 'Adversarial' }),
  () => agent(CONTEXT + '\n\nYou are a hostile SKEPTIC. Try to BREAK the designs and the premise. Attack hardest at: (1) LLM nondeterminism + a model that updates under you: does "re-run the codebase on the tape and compare" yield usable signal or noise, and when is replay-diff meaningless? (2) Is optimizing over code-space tractable, or is the evaluator so expensive/noisy the loop never converges? What is the realistic restricted search space? (3) Is max-capture an unshippable honeypot that kills the trust moat the strategy depends on? (4) Does "mutable objective over old tapes" actually hold, or do new objectives need signal the old capture never recorded (forcing re-capture)? (5) Distribution shift: re-running a NEW codebase diverges from the OLD trajectory immediately, so the human verdicts attached to the old trajectory may not transfer; does the whole approach collapse here, and what salvages it? For each attack: fatal, mitigable (how), or acceptable. Deliver the SURVIVING design constraints after the assault.', { label: 'skeptic', phase: 'Adversarial' }),
  () => agent(CONTEXT + '\n\nYou are a COMPLETENESS critic. Given the requirements and designs, name what is MISSING or under-specified that a real build hits: schema versioning/migration; time/clock + global ordering across the three distributed transports; whether the model RAW prompt/completion is even accessible through the claude terminal and the fallback if not; tool-result reproducibility for forked replay; durably linking a human verdict to the exact situation+action; cost/volume estimates per user-day; the on-device vs cloud re-sim execution boundary; right-to-delete propagating into the corpus and trained/eval artifacts; how the loop avoids overfitting the evaluator (Goodhart). List concrete gaps + a recommended resolution for each.\n\nREQUIREMENTS:\n' + requirements + '\n\nDESIGNS:\n' + designsText, { label: 'completeness', phase: 'Adversarial' }),
])
const critiqueText = ['JUDGE', 'SKEPTIC', 'COMPLETENESS'].map((k, i) => '## ' + k + '\n\n' + (critiques[i] || '(no result)')).join('\n\n---\n\n')

phase('Synthesize')
log('Synthesizing the final recommendation.')
const final = await agent(CONTEXT + '\n\nYou are the lead architect writing the FINAL RECOMMENDATION for the BlitzOS founder. Synthesize everything below into a decisive, buildable design doc for the session tape, EXPLICITLY framed as downstream of the fleet auto-research loop. Resolve disagreements; take positions; do not hedge into a menu. Structure:\n\n1. Thesis in 3-5 sentences: what the tape IS, given the loop is the customer.\n2. The auto-research loop, concretely (propose -> re-sim -> score against mutable objectives -> select -> iterate) and the REALISTIC restricted search space (not "the whole codebase" naively).\n3. The replay strategy we commit to: which mode(s), the honest limits given fixed-but-updatable weights + nondeterminism, and how distribution shift / verdict-transfer is handled.\n4. WHAT THE TAPE CAPTURES: the record shape(s) + granularity (turn-level model I/O? snapshots? moments? verdicts?), each line justified by a loop requirement. Give the concrete schema.\n5. The MUTABLE OBJECTIVE LAYER: how objectives live as data/judges over the tape so they change without re-capture, and what raw capture this forces.\n6. Storage + egress + privacy posture preserving BOTH replay value AND the trust moat (consent, redaction at-write vs at-egress, content-addressing, schema evolution, right-to-delete, on-device vs cloud / federated).\n7. How this RECONCILES with or rewrites the existing spec 01 (keep / change / add).\n8. Milestones: a shippable build order, starting with what de-risks the LOOP fastest (the loop, not the capture, is the risk).\n9. Top open questions + the cheap experiments that resolve each.\n10. One paragraph: what we are NOT doing yet and why.\n\nTight and decisive, no em dashes. This goes straight to the founder.\n\n=== REQUIREMENTS ===\n' + requirements + '\n\n=== DESIGNS ===\n' + designsText + '\n\n=== CRITIQUES ===\n' + critiqueText, { label: 'final-recommendation', phase: 'Synthesize' })

return { final, requirements }